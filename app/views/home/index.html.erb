<h1 class="display-4 text-center">Bienvenido a nuestra plataforma de Inmobiliaria</h1>
<p class="lead text-center">Explora propiedades destacadas y encuentra la casa de tus sueños.</p>

<div class="row">
  <% @properties.each do |property| %>
    <div class="col-md-4 mb-4">
      <div class="card">
        <%= image_tag property.image.attached? ? url_for(property.image) : "https://via.placeholder.com/500x300?text=Propiedad", class: "card-img-top", alt: property.title %>
        <div class="card-body">
          <h5 class="card-title"><%= property.title %></h5>
          <p class="card-text"><%= truncate(property.description, length: 100) %></p>
          <a href="<%= property_path(property) %>" class="btn btn-primary">Ver detalles</a>
        </div>
      </div>
    </div>
  <% end %>
</div>
